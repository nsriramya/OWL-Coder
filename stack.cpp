#include<bits/stdc++.h>
using namespace std;
int main()
{
	stack<int>s;
	s.push(1);
	s.emplace(8);
	s.push(7);
	s.emplace(2);
	s.push(6);
	s.emplace(3);
	s.push(0);
	s.emplace(8);
	while(!s.empty())
	{
	cout<<s.top()<<" ";
	s.pop();
}
	s.size();
	stack<int>s1,s2;
	s1.swap(s2);
	 
	 	 }
